name: zinit update
where:
  - dotfiles
run:
  - shell: |-
      source ~/.zshrc
      zinit self-update
      zinit update
      nvim --headless -c 'autcmd User PackerComplete quitall' -c 'PackerSync'
on-fail: email
