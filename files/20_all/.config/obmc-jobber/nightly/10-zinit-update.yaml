name: zinit update
where:
    - dotfiles
run:
    - shell: |-
          source ~/.zshrc
          (zinit self-update ; zinit update) || true
          cargo-sync
          npm-sync
          nvim-lazy-sync
          uv-sync
on-fail: email
